<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link rel="stylesheet" href="/styles/main.css">
    <title>Ubicacion</title>
</head>
<body>
    <div class="container">
        <h1 class="page-title">Tu ubicación</h1>
        <% if (location) { %>
            <p class="mb-2">Ubicación guardada: <strong><%= location %></strong></p>
        <% } %>
        <form action="/ubicacion" method="post">
            <div>
                <label>Ingresá tu ubicación</label>
                <input type="text" name="location" placeholder="Ciudad, barrio o dirección" value="<%= location || '' %>">
            </div>
            <button type="submit">Guardar</button>
        </form>
    </div>
</body>
</html>